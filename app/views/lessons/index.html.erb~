<h1>Listing lessons</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Info</th>
    <th>Published</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @lessons.each do |lesson| %>
  <tr>
    <td><%= lesson.title %></td>
    <td><%= lesson.info %></td>
    <td><%= lesson.published %></td>
    <td><%= link_to 'Show', course_lesson_path(@course, lesson) %></td>
    <td><%= link_to 'Edit', edit_course_lesson_path(@course, lesson) %></td>
    <td><%= link_to 'Destroy', course_lesson_path(@course, lesson), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Lesson', new_course_lesson_path(@course) %>
